name: absher_insight
description: Absher Insight MVP application
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  qr_flutter: ^4.1.0
  mobile_scanner: ^3.5.5
  animate_do: ^3.3.4
  intl: ^0.19.0
  flutter_spinkit: ^5.2.0
  local_auth: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true